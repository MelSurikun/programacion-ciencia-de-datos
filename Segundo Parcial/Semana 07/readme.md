Tareas de la semana 7:

Sesión 19:
✅ Tarea
Crea un notebook llamado manejo_archivos.ipynb que incluya:
1. Ejemplos de lectura y escritura de archivos .txt.
2. Ejemplos de lectura y escritura de archivos .csv.
3. Uso del bloque with open().
4. Un ejercicio propio que combine ambos tipos de archivos (por ejemplo, leer datos de un .txt y guardarlos como .csv).

💡 Extra: usa try/except para capturar errores si el archivo no existe (próxima sesión).

Sesión 20:
✅ Tarea
Crea un notebook llamado excepciones.ipynb que incluya al menos 3 programas con manejo de errores y excepciones:
1. Conversión de tipos y validación numérica.
2. Lectura de archivos con control de errores.
3. Excepción personalizada lanzada con raise.

💡 Extra: incluye un ejemplo con else y finally para demostrar control total del flujo.

Sesión 21:
✅ Tarea
Crea un notebook llamado poo_basica.ipynb que contenga al menos 2 clases propias, por ejemplo:
1. Coche: atributos (marca, modelo, año), método arrancar().
2. Libro: atributos (título, autor, páginas), método descripcion().
3. Empleado: atributos (nombre, salario), método aumento(porcentaje).

Cada clase debe incluir:
- Un método __init__ (constructor).
- Al menos un método adicional.
- Ejemplos de instanciación y uso.
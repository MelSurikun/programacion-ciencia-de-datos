Tareas de la semana 6:

Sesión 16:
✅ Tarea
Crea un notebook llamado funciones_basicas.ipynb que contenga 5 funciones diferentes (matemáticas, cadenas, listas):
1. Función matemática (por ejemplo: cuadrado(n), factorial(n)).
2. Función con cadenas (por ejemplo: invertir(texto), contar_vocales(texto)).
3. Función con listas (por ejemplo: ordenar_lista(nums), maximo_lista(nums)).
4. Función lógica (por ejemplo: es_par(n), es_primo(n)).
5. Una función propia (elige un problema sencillo y descríbelo).

Cada función debe incluir:
- Una breve descripción (docstring).
- Ejemplos de uso y salida.
- Pruebas simples (asserts o prints).

Sesión 17:
✅ Tarea
Crea un notebook llamado parametros_alcance.ipynb con al menos 3 funciones que usen valores por defecto.
Sugerencias de funciones:
1. potencia(base, exponente=2)
2. precio_con_iva(precio, iva=0.16)
3. convertir_temp(valor, a='F')
4. descuento(precio, porcentaje=0.10)
5. formatear_nombre(nombre, apellido, mayus=False)

Incluye para cada función:
- Docstring con descripción y parámetros.
- Ejemplos de uso y salidas esperadas.
- Pruebas simples (assert o impresiones).
- Bonus: explica en una celda Markdown por qué evitar mutables como valores por defecto.

Sesión 18:

✅ Tarea (para entregar)
Crea un notebook llamado funciones_modulares.ipynb con un mini-proyecto:
- Define un conjunto de funciones para limpiar datos (eliminar duplicados, reemplazar valores nulos, convertir tipos).
- Pruébalas con un dataset pequeño en CSV (puedes partir del que generamos aquí o crear uno propio).
- Usa docstrings en todas tus funciones.
- Muestra estadísticas (media, mediana, moda y rango) de al menos una columna numérica.



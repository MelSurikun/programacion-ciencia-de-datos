Tareas de la semana 9:

Sesión 25:
🧩 Tarea
Entrega un notebook llamado numpy_estadisticas_avanzadas.ipynb con:
1. Análisis de un dataset aleatorio 10x10: media, varianza, std, min, max por axis y globales.
2. Cálculo de percentiles (10, 25, 50, 75, 90) y breve interpretación.
3. Experimento de correlación: genera tres variables (A, B, C) donde B dependa linealmente de A y C sea ruido; calcula y comenta la matriz de correlación 3x3.

💡 Sugerencia: comenta brevemente cómo cambia el resultado al variar la distribución (rand, randn, randint).

Sesión 26:
🧮 Tarea
Entrega un notebook llamado numpy_broadcasting.ipynb que incluya al menos 3 ejemplos propios de operaciones con broadcasting. Ideas:
- Resta de medias por columna/filas, escalado por vector.
- Generar una malla de coordenadas X, Y y evaluar una función f(X,Y).
- Estandarizar por filas en lugar de columnas.
- Aplicar un vector de pesos por columna a una matriz de datos.

💡 Incluye una breve explicación de por qué el broadcasting funciona en cada ejemplo (formas y ejes).

Sesión 27:
🧩 Tarea (mini‑proyecto)
Entrega un notebook llamado numpy_simulaciones.ipynb que incluya:
1. Simulación de un experimento aleatorio (elecciones, cartas, lotería o similar).
2. Análisis con estadísticas (medidas de tendencia y dispersión) y gráficas simples.
3. Una breve conclusión interpretando los resultados y limitaciones del modelo.

💡 Puedes usar np.random.choice, np.random.randint, np.random.rand, np.random.randn según la naturaleza de tu experimento.
Tareas de la semana 14:

Sesión 40:
📌 Tarea
Crear un notebook llamado seaborn_basico.ipynb que incluya al menos 3 gráficos propios usando Seaborn, por ejemplo:
- Un histplot para analizar la distribución de una variable numérica.
- Un countplot para ver la frecuencia de categorías.
- Un barplot con agregaciones (promedios, sumas u otra métrica).

Se valora que:
- Utilices distintos estilos (sns.set_style).
- Agregues títulos, etiquetas de ejes y, cuando aplique, leyendas.

Sesión 41:
📌 Tarea
Crear un notebook llamado seaborn_eda.ipynb que incluya al menos 3 visualizaciones aplicadas a un dataset real, por ejemplo:
- Un scatterplot para analizar la relación entre dos variables numéricas.
- Un boxplot o violinplot para comparar distribuciones por categoría.
- Un heatmap de correlación para variables numéricas.

Se valora que:
- Expliques brevemente qué observas en cada gráfico.
- Identifiques posibles patrones, tendencias o outliers.

Sesión 42:
📌 Tarea
Crear un notebook llamado seaborn_proyecto.ipynb con un mini-EDA visual completo de un dataset de libre elección.

Requisitos mínimos:
- Al menos 5 visualizaciones diferentes (por ejemplo: histograma, boxplot, scatterplot, heatmap, pairplot).
-  Uso de al menos una de estas funciones avanzadas:
sns.pairplot()
sns.lmplot()
- FacetGrid o sns.catplot()
- Breve explicación escrita de lo que observas en cada gráfico.
- El notebook debe estar listo para formar parte del Proyecto Final (Bloque III).